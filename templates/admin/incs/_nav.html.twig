<div id="app" class="container">
        <nav class="navbar navbar-expand-lg navbar-dark  bg-primary">
                        <a class="navbar-brand logo" href="{{ path('homepage') }}">Sakannat</a>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse " id="navbarSupportedContent">
                        <!-- Left Side Of Navbar -->
                        <ul class="navbar-nav mr-auto">
                                <li class="nav nav-item">
                                    <a href="#" class="nav-link"> <i class="fas fa-chart-line"></i>Dashboard</a>
                                </li>
                                <li>
                                    <a href="{{ path('admin_ads_index') }}" class=" nav-link">
                                    <i class="fas fa-home"></i>Announces<i class="fas fa-hotel"></i>
                                    </a>
                                </li>
                                 <li>
                                    <a href="#" class=" nav-link"><i class="fas fa-folder"></i>Reservation</a>
                                </li>
                                <li>
                                    <a href="#" class=" nav-link"> <i class="fas fa-object-group"></i>Agencies</a></li>
                                <li>
                                    <a href="#" class=" nav-link">
                                    <i class="fas fa-comments"></i>Comments</a>
                                </li>
                                <li>
                                    <a href="#" class=" nav-link"> <i class="fas fa-users"></i> Users</a>
                                </li>
                        </ul>
                        <ul class="navbar-nav ml-auto ">
                             <li class="nav-item">
                                <a href="{{ path('homepage') }}" class="nav-link">
                                <i class="fas fa-eye"></i>
                                Look at the site</a>
                            </li>
                        {% if app.user %}
                            <li class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                                    <img src="{{ app.user.picture }}" class="avatar avatar-mini" 
                                    alt="Avatar of {{ app.user.fullName }}"> {{ app.user.fullName }}
                                </a>
                                
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                                <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Logout</a>
                            </div>
                            </li>
                        {% endif %}
                        </ul>
                    </div>
                    
                    </div>
            </nav>
    </div>
    <hr>
    {% block body %}{% endblock %}