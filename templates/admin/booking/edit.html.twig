{% extends 'admin/base.html.twig' %}

{% block title %}Reservations Modifications !{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5">Modifications Of Reservation No° : {{ booking.id }} As Admin</h1>
        {{ form_start(form) }}
            <div class="row">
                <div class="col">
                    {{ form_row(form.startDate) }}
                    {{ form_row(form.endDate) }}
                    {{ form_row(form.comment) }}
                </div>
                <div class="col">
                    {{ form_row(form.booker) }}
                    {{ form_row(form.ad) }}
                     <button type="submit" class="btn btn-success">
                        <i class="fas fa-check"></i>
                        Save Your Modifications !
                    </button>
                </div>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
