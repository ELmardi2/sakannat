<div id="app" class="container">
        <nav class="navbar navbar-expand-md navbar-light navbar-laravel bg-info">
                        <a class="navbar-brand logo" href="{{ path('homepage') }}">Sakannat</a>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse " id="navbarSupportedContent">
                        <!-- Left Side Of Navbar -->
                        <ul class="navbar-nav m-auto float">
                                <li class="nav nav-item"><a href="{{ path('homepage') }}" class="nav-link">Home</a></li>
                                <li><a href="{{ path('ads_index') }}" class=" nav-link">Announces</a></li>
                                 <li><a href="#" class=" nav-link">About-us</a></li>
                                <li><a href="#" class=" nav-link">Agencies</a></li>
                                <li><a href="#" class=" nav-link">Contact-us</a></li>
                        </ul>
                        <ul class="navbar-nav ml-auto ">
                        {% if app.user %}
                            <li class="nav-item">
                                <a href="{{ path('account_logout') }}" class="link nav-link">Logout</a>
                            </li>
                        {% else %}
                             <li class="nav-item">
                                <a href="{{ path('account_register') }}" class="nav-link">Inscription</a>
                            </li>
                            &nbsp;
                             <li class="nav-item ">
                                <a href="{{ path('account_login') }}" class="link nav-link">Login</a>
                            </li>
                        {% endif %}
                        </ul>
                    </div>
            </nav>
    </div>
    <hr>
    {% block body %}{% endblock %}