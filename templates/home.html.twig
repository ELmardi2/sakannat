{% extends 'base.html.twig' %}
{% block title %} Homepage{% endblock %}
{% block header %}
    <h3>Hello from header</h3>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron">
        <h1 class="text-center">{{ title}}</h1>
            <div class="row">
                <div class="col">
                    <article>
                        Welcome to you in our website, We make you find your home to reside very easy or
                        to get a benifit from your house's space or even if you want to travel, you can
                        rente yours and hire another house in your distination
                    </article>
                </div>
                <div class="col">
                  <p class="lead">
                        مرحبًا بك في موقعنا على الويب ، فنحن نجعلك تجد منزلك مكانًا سهلاً للغاية أو
                        للحصول على منفعة من مساحة منزلك أو حتى إذا كنت ترغب في السفر ، يمكنك ايجار
                        منزلك واستئجار منزل آخر في منطقتك.
                  </p>
                </div>
            </div>
        </div>
        <hr>
        <h2 class="H1 my-5">Our Appartements And Houses </h2>
        <div class="row mb-5">
            {% for data in ads %}
                {% include 'ad/_ad.html.twig' with {'ad': data.announce} %}
            {% endfor %}
        </div>
        <hr>
        <h2 class="H1 my-5">Our Owners's stars</h2>
        <div class="row">
        {% for data in users %}
            <div class="col-md-6">
              <div class="card border-secondary mb-3">
                 <div class="card-body">
                        <h4 class="cart-title">{{ data.user.fullName }}</h4>
                        <hr>
                     <div class="card-text">
                         <img src="{{ data.user.picture }}" alt="{{ data.user.fullName }}" class="float-left mr-3">
                            <p>
                           {{  data.user.introduction }}
                            </p>
                            <a href="{{ path('user_show',{'slug': data.user.slug}) }}" class="btn btn-primary float-right">Read More +</a>
                     </div>
                 </div>
              </div>
            </div>
        {% endfor %}
            
        </div>
    </div>
{% endblock %}